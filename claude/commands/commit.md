---
disable-model-invocation: true
description: Generate and execute a git commit using the agent framework
allowed-tools: mcp__agent-framework__commit
---

CRITICAL: Do NOT run any bash commands, git commands, or use any tools other than mcp__agent-framework__commit.
Do NOT run git status, git add, git diff, or any other preparatory git operations.

IMMEDIATELY call the mcp__agent-framework__commit tool without any preparation steps.

Report the result to the user.
