---
name: agent-framework-confirm
description: Analyze uncommitted changes and confirm code quality through the agent-framework confirm MCP tool. Use when the user invokes the agent-framework confirm workflow or asks for the old /confirm command equivalent.
---

# Agent Framework Confirm

Use only `mcp__agent-framework__confirm`. Do not use raw git commands.

Call `mcp__agent-framework__confirm` with `working_dir` set to the current working directory, then report the result to the user.

