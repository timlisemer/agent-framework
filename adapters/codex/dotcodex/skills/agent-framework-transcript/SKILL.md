---
name: agent-framework-transcript
description: Print the absolute path to the current Codex session transcript via the agent-framework transcript MCP tool. Use when the user invokes the agent-framework transcript workflow or asks for the old /transcript command equivalent.
---

# Agent Framework Transcript

1. Immediately call `mcp__agent-framework__transcript` with no parameters.
2. Do not run Bash commands or read files first.
3. Print the returned path string verbatim.

