# Fixtures

Curated real JSONL transcripts for hook replay testing.

## Adding Fixtures

1. Copy a transcript from `~/.claude/projects/` into `test-harness/fixtures/transcripts/`
2. Use `npx tsx test-harness/run.ts --list <path>` to identify testable moments
3. Document each fixture below with its test scenarios

## Fixture Index

(No fixtures yet — add transcripts and document them here.)
