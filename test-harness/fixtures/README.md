# Fixtures -- Transcript Format

The JSONL transcript format used by the replay harness is documented in the MCP tool help system. Use the `help` action on `test_harness_labeler` or `test_harness_tester` for complete format reference.

Transcripts are read directly from `~/.claude/projects/<project>/*.jsonl` -- do not copy or curate transcripts into this directory.
